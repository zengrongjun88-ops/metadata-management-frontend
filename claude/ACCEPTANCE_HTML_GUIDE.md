# 独立HTML验收页面说明

## 📄 文件说明

**文件名**: `acceptance.html`

这是一个**完全独立的HTML文件**，包含了元数据管理系统的所有核心功能和Mock数据，无需任何服务器或构建工具，可以直接在浏览器中打开进行功能验收。

---

## 🚀 使用方法

### 方式一：直接双击打开
1. 找到 `acceptance.html` 文件
2. 双击文件，在默认浏览器中打开
3. 开始验收

### 方式二：从浏览器打开
1. 打开浏览器（Chrome、Firefox、Safari、Edge）
2. 拖拽 `acceptance.html` 文件到浏览器窗口
3. 开始验收

### 方式三：使用命令行打开
```bash
# macOS
open acceptance.html

# Linux
xdg-open acceptance.html

# Windows
start acceptance.html
```

---

## ✨ 功能特性

### 完全独立运行
- ✅ **无需Node.js** - 不需要npm install
- ✅ **无需服务器** - 不需要npm run dev
- ✅ **无需数据库** - 所有数据内置在HTML中
- ✅ **无需网络** - 所有依赖从CDN加载（首次需要网络，之后浏览器会缓存）

### 包含的功能模块

#### 1. 元数据表管理 🗄️
- ✅ 查看10个测试表（覆盖6种数据源）
- ✅ 搜索表名、库名、描述
- ✅ 查看表详情和字段信息
- ✅ 显示敏感等级、重要等级、数仓分层等属性

#### 2. 审批管理 ✅
- ✅ 查看6个审批单（5种状态）
- ✅ 状态筛选（待审批、已通过、已拒绝、已取消、已发布）
- ✅ 执行审批操作（通过）
- ✅ 发布已通过的审批单

#### 3. 操作历史 📜
- ✅ 查看10条操作记录
- ✅ 按操作类型筛选（CREATE、UPDATE、DELETE）
- ✅ 显示操作详情

---

## 📊 内置测试数据

| 数据类型 | 数量 | 说明 |
|---------|-----|------|
| 元数据表 | 10个 | Hive、BigQuery、ClickHouse、Paimon、Iceberg、StarRocks |
| 字段定义 | 15+个 | user_info表6个字段，order_detail表4个字段 |
| 审批流程 | 6个 | 待审批、已通过、已拒绝、已取消、已发布 |
| 操作历史 | 10条 | CREATE和UPDATE操作记录 |

---

## 🎯 验收清单

### 元数据管理（5项）
- [ ] 1. 打开页面，默认显示"元数据管理"，表格显示10条数据
- [ ] 2. 在搜索框输入"user"，应显示3条结果（user_info、user_behavior_log、customer_profile）
- [ ] 3. 点击"user_info"表名，查看详情，应显示6个字段
- [ ] 4. 查看字段的主键、加密、敏感等级标识
- [ ] 5. 关闭详情模态框

### 审批管理（5项）
- [ ] 6. 点击左侧菜单"审批管理"，显示6个审批单
- [ ] 7. 使用状态筛选，选择"待审批"，应显示1条（APPR-2024-001）
- [ ] 8. 点击待审批记录的"审批"按钮，弹出审批表单
- [ ] 9. 填写审批人"admin"，审批意见"同意"，点击"通过"
- [ ] 10. 观察该记录状态变更为"已通过"，显示绿色标签

### 操作历史（3项）
- [ ] 11. 点击左侧菜单"操作历史"，显示10条记录
- [ ] 12. 使用操作类型筛选，选择"CREATE"，应显示6条CREATE操作
- [ ] 13. 观察操作类型标签颜色（CREATE绿色、UPDATE蓝色）

### UI/UX（3项）
- [ ] 14. 点击左上角折叠按钮，菜单正常折叠和展开
- [ ] 15. 界面使用Ant Design风格，美观统一
- [ ] 16. 所有交互流畅，无JavaScript错误（F12查看Console）

**总计：16项验收要点**

---

## 🎨 技术实现

### 依赖库（CDN加载）
- **React 18.2.0** - 使用UMD版本
- **ReactDOM 18.2.0** - 渲染引擎
- **Ant Design 5.12.0** - UI组件库
- **Babel Standalone** - 浏览器内JSX编译
- **Day.js** - 日期处理（Ant Design依赖）

### 代码结构
```
acceptance.html
├── HTML结构
├── CSS样式（内联）
├── JavaScript代码（Babel编译）
│   ├── Mock数据（mockTables、mockApprovals、mockHistory）
│   ├── 工具函数（格式化、颜色映射等）
│   ├── TableList组件（元数据管理）
│   ├── ApprovalList组件（审批管理）
│   ├── OperationHistory组件（操作历史）
│   └── App组件（主应用）
└── 渲染逻辑
```

---

## 🔧 自定义修改

如需修改数据或功能，可以直接编辑HTML文件：

### 修改Mock数据
在 `<script type="text/babel">` 标签内找到：
- `mockTables` - 修改表数据
- `mockApprovals` - 修改审批数据
- `mockHistory` - 修改历史数据

### 添加新功能
1. 在相应组件中添加新的处理逻辑
2. 保存文件
3. 刷新浏览器即可看到效果

---

## ⚠️ 注意事项

### 首次打开需要网络
- CDN依赖需要从网络加载（React、Ant Design等）
- 加载后浏览器会缓存，后续可离线使用

### 浏览器兼容性
推荐使用以下浏览器：
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

### 数据持久化
- 所有操作（审批、筛选等）的状态变更仅保存在内存中
- **刷新页面会重置所有数据**
- 这是正常现象，因为是纯前端验收页面

---

## 📝 与完整项目的区别

| 特性 | acceptance.html | 完整项目 |
|-----|----------------|---------|
| 启动方式 | 双击打开 | npm run dev |
| 依赖安装 | 无需 | npm install |
| 构建工具 | 无需 | Vite |
| 数据持久化 | 内存（刷新重置） | Mock模式内存，可切换真实API |
| 功能完整度 | 核心功能 | 所有功能（含创建表、编辑等） |
| 代码组织 | 单文件 | 模块化目录结构 |
| 类型安全 | 无 | TypeScript |
| 用途 | **快速验收展示** | **开发和生产** |

---

## 🎯 适用场景

### ✅ 适合
- 快速功能演示
- 离线环境验收
- 发给非技术人员查看
- 不想配置开发环境
- 临时测试和预览

### ❌ 不适合
- 生产环境使用
- 需要频繁修改代码
- 需要复杂的开发调试
- 需要连接真实后端API

---

## 💡 验收建议

### 开发者工具
1. 按 **F12** 打开开发者工具
2. 查看 **Console** 面板，确认无错误
3. 查看 **Network** 面板，观察CDN资源加载

### 测试流程
1. 先测试元数据管理（搜索、详情）
2. 再测试审批管理（审批操作、状态变更）
3. 最后测试操作历史（筛选）
4. 测试UI交互（菜单折叠、模态框等）

---

## 🚀 快速开始

```bash
# 1. 克隆仓库
git clone https://github.com/zengrongjun88-ops/metadata-management-frontend.git

# 2. 进入目录
cd metadata-management-frontend

# 3. 打开HTML文件
open acceptance.html
# 或双击文件直接打开

# 就这么简单！
```

---

## 📞 问题反馈

如在使用过程中遇到问题：
1. 检查浏览器版本是否过旧
2. 确认网络连接正常（首次打开）
3. 打开开发者工具查看错误信息
4. 尝试清除浏览器缓存后重新打开

---

## 🎉 优势总结

相比完整项目的优势：
- ✅ **零配置** - 无需安装任何依赖
- ✅ **即开即用** - 双击就能打开
- ✅ **易分享** - 单个文件，方便发送
- ✅ **跨平台** - 任何操作系统的浏览器都可以打开
- ✅ **离线可用** - CDN缓存后可离线使用

---

**享受便捷的验收体验！** 🎉
